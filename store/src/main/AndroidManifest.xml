<?xml version="1.0" encoding="utf-8"?>
<!--
  ~ Copyright (c) 2021-2026. NICE Ltd. All rights reserved.
  ~
  ~ Licensed under the NICE License;
  ~ you may not use this file except in compliance with the License.
  ~ You may obtain a copy of the License at
  ~
  ~    https://github.com/nice-devone/nice-cxone-mobile-sdk-android/blob/main/LICENSE
  ~
  ~ TO THE EXTENT PERMITTED BY APPLICABLE LAW, THE CXONE MOBILE SDK IS PROVIDED ON
  ~ AN “AS IS” BASIS. NICE HEREBY DISCLAIMS ALL WARRANTIES AND CONDITIONS, EXPRESS
  ~ OR IMPLIED, INCLUDING (WITHOUT LIMITATION) WARRANTIES OF MERCHANTABILITY,
  ~ FITNESS FOR A PARTICULAR PURPOSE, NON-INFRINGEMENT, AND TITLE.
  -->

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.HIDE_OVERLAY_WINDOWS" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".StoreApplication"
        android:allowAudioPlaybackCapture="false"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/StoreFrontTheme"
        android:useEmbeddedDex="true"
        tools:targetApi="31">
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="com.nice.cxonechat.sample.ChatInitializer"
                android:value="androidx.startup" />
        </provider>

        <activity
            android:name=".StoreActivity"
            android:exported="true"
            android:intentMatchingFlags="enforceIntentFilter"
            android:label="@string/title_activity_store_front"
            android:theme="@style/StoreFrontTheme"
            tools:targetApi="36">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- This setup is required only to register correct intent filter for the push notifications. -->
        <activity
            android:name="com.nice.cxonechat.ui.screen.ChatActivity"
            android:exported="true"
            android:intentMatchingFlags="enforceIntentFilter"
            tools:targetApi="36">
            <!-- The intent filter is required by the deeplinks in the push notifications. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <!-- The intent filter data scheme has to match the scheme set in the channel settings. -->
                <data android:scheme="com.nice.cxonechat.sample" />
            </intent-filter>
        </activity>

        <!-- AppAuth redirect activity for OAuth authorization code flow.
             Scheme must be the reverse of the Android client ID — required by Google for custom URI schemes. -->
        <activity
            android:name="net.openid.appauth.RedirectUriReceiverActivity"
            android:exported="true"
            tools:node="replace">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="com.googleusercontent.apps.380097439409-5jigbr2e8beu7qkpah2u2ja1vtjmunu5" />
            </intent-filter>
        </activity>

        <!-- Trigger Google Play services to install the backported photo picker module. -->
        <service
            android:name="com.google.android.gms.metadata.ModuleDependencies"
            android:enabled="false"
            android:exported="false"
            tools:ignore="MissingClass">
            <intent-filter>
                <action android:name="com.google.android.gms.metadata.MODULE_DEPENDENCIES" />
            </intent-filter>
            <meta-data
                android:name="photopicker_activity:0:required"
                android:value="" />
        </service>

        <provider
            android:name=".LogFileProvider"
            android:authorities="com.nice.cxonechat.logprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/log_path" />
        </provider>
    </application>

</manifest>
