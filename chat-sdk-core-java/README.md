# Module chat-sdk-core-java

The `chat-sdk-core-java` module provides a Java-friendly API for the CXone Chat SDK,
which is primarily written in Kotlin and uses coroutines.
This module allows Java developers to interact with the SDK without needing to manage coroutines directly.
